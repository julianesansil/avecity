import React from 'react';
import { View, Text } from 'react-native';

const NewLocation = () => {
  return (
    <View style={{ justifyContent: 'center', alignItems: 'center' }}>
      <Text>Nova Localidade</Text>
    </View>
  );
};

export default NewLocation;
